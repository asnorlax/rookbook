# Clean, shareable player URLs: /player/<chess.com-username> serves the app (URL stays put),
# and the app reads the username from the path and auto-loads it — no manual fetch.
/player/*   /app.html   200
