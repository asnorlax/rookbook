# Claude Code local project data — not part of the site
.claude/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.swp

# Netlify local dev
.netlify/

# Installed by Netlify so netlify/functions can use @netlify/blobs. publish="." means the
# repo root IS the served directory, so this must never be committed — see _redirects for the
# belt-and-braces 404 in case the build host leaves a copy behind.
node_modules/

# Local test artifacts — never deploy these
_calib.html
_tmp-*
*.bak-*
